/*
Agregar variables a cadenas

Así como podemos construir una cadena en varias líneas a partir de literales de cadena, 
también podemos agregar variables a una cadena usando el += operador más igual ().

Ejemplo:

const anAdjective = "awesome!";
let ourStr = "freeCodeCamp is ";
ourStr += anAdjective;

ourStr tendría el valor freeCodeCamp is awesome!.

Establézcalo someAdjective en una cadena de al menos 3 caracteres y agréguelo al myStr uso del += operador.

*/

const someAdjective = "Change code below";
let myStr = "Learning to code is ";
myStr += someAdjective;
