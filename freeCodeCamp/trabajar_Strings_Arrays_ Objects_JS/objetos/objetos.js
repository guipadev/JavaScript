/**
 * Cómo usar objetos en JavaScript
 * Si desea ser un buen desarrollador de JavaScript, realmente debe tener una comprensión
 * decente de los objetos y la forma en que funcionan.
 *
 * Casi todos los objetos que crea en JavaScript heredan métodos del Object prototipo global
 * que está disponible globalmente para cada objeto en JavaScript.
 * Una excepción son los objetos prototipo nulos.
 * Todos los métodos de los que voy a hablar son en su mayoría estáticos.
 *
 * Primero, hablemos sobre cómo crear objetos usando un conjunto de llaves { }
 * o el constructor de objetos . Esto es lo que parece:
 */

// Creating an object from an constructor

let obj1 = Object();

// Creating an object using curly braces

let obj2 = {};
