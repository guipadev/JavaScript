//No es una caracteristica de JavaScript, si no una forma de simplificar de como regresamos objetos

function getPosts(url) {
    // fetch
    // process

    return {
        id: 5,
        title: 'Post 01'
    }
}

//Evitar declarar variables y luego regresandolas