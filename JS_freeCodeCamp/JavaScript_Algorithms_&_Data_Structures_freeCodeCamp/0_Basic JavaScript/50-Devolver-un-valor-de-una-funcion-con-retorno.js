/*
Devolver un valor de una función con retorno

Podemos pasar valores a una función con argumentos. Puede usar una return declaración para enviar un valor de vuelta fuera de una función.

Ejemplo

function plusThree(num) {
  return num + 3;
}

const answer = plusThree(5);

answertiene el valor 8.

plusThree toma un argumento a favor numy devuelve un valor igual a num + 3.

Cree una función timesFive que acepte un argumento, lo multiplique por 5 y devuelva el nuevo valor.
*/

function timesFive(num) {
    return num * 5;
}

timesFive(5);
